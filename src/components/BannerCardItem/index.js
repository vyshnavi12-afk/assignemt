// Write your code here.
let bannercarditem=(props)=>{
     return(
        const {id,headerText,description,className}=props;
        <li className={className}>
            <h1>{headerText}</h1>
            <p>{description}</p>
        </li>
)
     }